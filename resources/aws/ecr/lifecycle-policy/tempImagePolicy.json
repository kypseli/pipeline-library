{
    "rules": [
        {
            "rulePriority": 1,
            "description": "Expire images older than 1 hour",
            "selection": {
                "tagStatus": "tagged",
                "tagPrefixList": ["${tag}"],
                "countType": "sinceImagePushed",
                "countUnit": "days",
                "countNumber": 1
            },
            "action": {
                "type": "expire"
            }
        }
    ]
}
